interface:
  display_name: "HyperFrames Screen Packaging"
  short_description: "先锁定素材关系与交付映射，再完成可验证的视频包装"
  brand_color: "#00A9CD"
  default_prompt: "Use $hyperframes-screen-packaging to validate source relationships and delivery scope first, then package every required video into its correctly mapped final output."
